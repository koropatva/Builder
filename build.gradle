apply plugin: 'groovy'
apply plugin: 'maven'

dependencies {
	compile gradleApi()
	groovy localGroovy()
}
repositories {
	mavenCentral()
}

group='com.hmel'
version='0.1-SNAPSHOT'

apply plugin: 'maven'
uploadArchives {
	repositories {
		mavenDeployer {
			repository(url: uri('../PhoneDictionary/libs'))
		}
	}
}
